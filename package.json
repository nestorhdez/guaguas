{
  "name": "lambda-guaguas",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:src; npm run build:lib; npm run build:zipped",
    "build:src": "cd src; npm i; cd ..; tsc",
    "build:lib": "cd lib; npm i; cd ..",
    "build:zipped": "mkdir lambda; zip -r lambda/lib.zip lib",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "tslint": "^6.1.1",
    "typescript": "^3.8.3"
  }
}
