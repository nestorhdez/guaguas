{
  "name": "lambda-guaguas",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc; npm run build:zipped",
    "build:zipped": "cd lib; npm i; cd ..; zip -r ./lambda/bus.zip lib",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "netlify-lambda": "^1.6.3"
  },
  "devDependencies": {
    "tslint": "^6.1.1",
    "typescript": "^3.8.3"
  }
}
